#include <stdio.h>

int main()
{

// Instead of using a traditional if statement you can use a shorthand if statement
	
// variable = (condition) ? expressionTrue : expressionFalse;
	int age = 18;
	(age <= 18) ? printf("The age is: %d\n", age) : printf("You are too old");
}
