#include <stdio.h>

int main()
{
	const int myNum = 15; // This is immutable and read-only
	myNum = 10; // This will trigger an error
	// Good practice with constants is to declare them with UPPERCASE variables

}

